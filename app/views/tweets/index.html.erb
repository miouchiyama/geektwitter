<h1>GeekTwitter</h1>
<%= button_to 'ログアウト', destroy_user_session_path, method: :delete %>
<h3>Tweet一覧</h3>
<%= link_to "新規投稿へ", tweets_new_path %>

<div class="tweets-bigcontainer">
<div class="tweets-container">
  <% @tweets.each do |t| %>
    <div class="tweet-cover">
      <div class="tweet">
        <%= t.user.email %>
        <%= t.body %>
        <%= t.created_at %>
        <%= link_to "詳細へ", tweet_path(t.id) %>
        <%= link_to "編集する", edit_tweet_path(t.id) %>
        <%= link_to "削除する", tweet_path(t.id), method: :delete %> 
      </div>
    </div>
  <% end %>
</div>
</div>
